%p#notice= notice
%h1 我的书架
%table
  %thead
    %tr
      -# %th{:colspan => "3"}
      %th
        书名
      %th 最近更新章节
      %th 更新时间
      %th 作者
      %th 操作
  %tbody
    - @books.each do |book|
      %tr
        %td= book.name
        %td= book.chapters.last.name
        %td= book.chapters.last.created_at.strftime('%F %R')
        %td= book.author_name
        %td= link_to '删除', book, method: :delete, data: { confirm: '确认删除？' }
%br
= link_to 'New Book', new_book_path

:css
  table {
    margin: auto;
  }
